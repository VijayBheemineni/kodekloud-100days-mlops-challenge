# Task Challenge
Setup 'venv' and install the required packages and freeze the packages to 'requirements.txt'

```
python3 -m venv ml-env
source bin/activate/ml-env
pip install numpy pandas scikit-learn matplotlib
pip freeze requirements.txt
```